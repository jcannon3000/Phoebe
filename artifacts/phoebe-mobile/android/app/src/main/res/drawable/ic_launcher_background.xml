<?xml version="1.0" encoding="utf-8"?>
<!-- Real Phoebe brand background — the same dark green (#112818) sampled
     from the app icon's own background, so the adaptive icon's inset
     foreground (see mipmap-*/ic_launcher_foreground.png) blends seamlessly
     into this layer with no visible seam. Replaces the Capacitor template's
     teal grid-pattern placeholder. -->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportHeight="108"
    android:viewportWidth="108">
    <path
        android:fillColor="#112818"
        android:pathData="M0,0h108v108h-108z" />
</vector>
