<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- App Links — Android's counterpart to iOS's associated-domains
                 universal links (App.entitlements). autoVerify makes Android
                 check https://withphoebe.app/.well-known/assetlinks.json at
                 install time; once verified, tapping a withphoebe.app link
                 anywhere on the device opens this app instead of the browser,
                 same as the iOS side already does. The web app's own router
                 (native-shell.ts's appUrlOpen listener, already
                 platform-agnostic — see devicePlatform()) handles the actual
                 path once Android delivers the Intent. No path restriction
                 here (Android verifies at the domain level); everything not
                 meaningfully deep-linkable just lands on the matching page. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="withphoebe.app" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Daily prayer reminders. Required from Android 13 (API 33): without it
         declared AND granted at runtime, notifications are silently never
         shown. Capacitor's PushNotifications.requestPermissions() raises the
         system dialog once the permission is declared here. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- The contemplation timer and the office chime buzz on transitions. -->
    <uses-permission android:name="android.permission.VIBRATE" />
</manifest>
