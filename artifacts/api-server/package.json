{
  "name": "@workspace/api-server",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "export NODE_ENV=development && export RUN_SCHEDULERS_IN_WEB=true && export PATH=/Applications/Postgres.app/Contents/Versions/latest/bin:/Users/jeremycannon/.local/bin:$PATH && pnpm --filter @workspace/mymonastery run build && BASE_PATH=/mail pnpm --filter @workspace/letters-app run build && pnpm run build && pnpm run start",
    "build": "node ./build.mjs",
    "//build:visio-schedule": "Regenerate the Visio schedule — ONE ARTWORK PER WEEK, keyed to the week's Sunday (per-year appearance cap, an appearance being a week). A .mjs (not .ts) because it imports .ts across workspaces, which api-server's rootDir forbids; bundled first; writes mymonastery/src/lib/visioSchedule.ts.",
    "//build:icon-schedule": "Regenerate the weekly SUGGESTED icon (third door on a new week), keyed to the Sunday the week closes on. Same .mjs-across-workspaces arrangement as the visio schedule; writes mymonastery/src/lib/iconSchedule.ts.",
    "build:icon-schedule": "esbuild src/build-icon-week-schedule.mjs --bundle --platform=node --format=esm --outfile=.icon-schedule.mjs && node .icon-schedule.mjs ../mymonastery/src/lib/iconSchedule.ts && rm -f .icon-schedule.mjs",
    "build:visio-schedule": "esbuild src/build-visio-week-schedule.mjs --bundle --platform=node --format=esm --outfile=.visio-schedule.mjs && node .visio-schedule.mjs ../mymonastery/src/lib/visioSchedule.ts && rm -f .visio-schedule.mjs",
    "build:spirituals-lectionary": "esbuild src/build-spirituals-lectionary.mjs --bundle --platform=node --format=esm --outfile=.spirituals-lectionary.mjs && node .spirituals-lectionary.mjs ../mymonastery/src/lib/spiritualsLectionary.ts && rm -f .spirituals-lectionary.mjs",
    "build:all": "pnpm --filter @workspace/mymonastery run build && BASE_PATH=/mail pnpm --filter @workspace/letters-app run build && node ./build.mjs",
    "start": "node --enable-source-maps ./dist/index.mjs",
    "start:worker": "node --enable-source-maps ./dist/worker.mjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "//test": "Node's built-in runner over esbuild-bundled tests. Bundled because the repo's imports are extensionless (bundler resolution), which Node's own resolver can't follow.",
    "test": "rm -rf .test-build && esbuild \"src/**/*.test.ts\" --bundle --platform=node --format=esm --outdir=.test-build --out-extension:.js=.mjs --log-level=error && node --test \".test-build/**/*.mjs\""
  },
  "dependencies": {
    "@sentry/node": "^8.55.2",
    "@workspace/api-zod": "workspace:*",
    "@workspace/db": "workspace:*",
    "@workspace/integrations-anthropic-ai": "workspace:*",
    "connect-pg-simple": "^10.0.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2",
    "drizzle-orm": "catalog:",
    "express": "^5",
    "express-session": "^1.19.0",
    "googleapis": "^171.4.0",
    "jose": "^5.9.6",
    "passport": "^0.7.0",
    "passport-google-oauth20": "^2.0.0",
    "pino": "^9",
    "pino-http": "^10",
    "web-push": "^3.6.7",
    "ws": "^8.20.1",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@types/connect-pg-simple": "^7.0.3",
    "@types/cookie-parser": "^1.4.10",
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/express-session": "^1.18.2",
    "@types/node": "catalog:",
    "@types/passport": "^1.0.17",
    "@types/passport-google-oauth20": "^2.0.17",
    "@types/pg": "^8.18.0",
    "@types/web-push": "^3.6.4",
    "@types/ws": "^8.18.1",
    "esbuild": "^0.27.3",
    "esbuild-plugin-pino": "^2.3.3",
    "pino-pretty": "^13",
    "thread-stream": "3.1.0"
  }
}
